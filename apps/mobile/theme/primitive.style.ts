import * as tokens from "@/theme/token.style";

export const border = {
  base: {
    borderRadius: tokens.borderRadius["br-11"],
    borderWidth: 1,
    borderColor: tokens.colors.b1,
  },
};

export const spacing = {
  base: {
    padding: tokens.spacing["sp-14"],
    marginBottom: tokens.spacing["sp-8"],
  },
};

export const colors = {
  main: {
    color: tokens.colors.orange,
  },
  proteine: {
    color: tokens.colors.blue,
  },
  glucide: {
    color: tokens.colors.yellow,
  },
  lipide: {
    color: tokens.colors.orange,
  },
};
